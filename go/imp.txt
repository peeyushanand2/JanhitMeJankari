funtion
Variadic parameter ... last of function
Literal: {}
- Array Literal
- Slice Literal
- Map Literal
- Struct Literal

for loop - use as while do while and for utilizing rang keyword.
    need both key and value
    for k, v := range data {
		fmt.Printf("Key: %s Value: %s\n", k, v)
	}
     need only value
    for _, v := range data {
		fmt.Printf("Value: %s\n", v)
	}
    need only key
    for k := range data {
		fmt.Printf("Key: %s", k)
	}

if  direction check and and sign in scope
if _, exist := data[key]; !exist {
		fmt.Println("key not exist")
		return
	}
   
len
cap
make
append
copy
---------------------------------------------------------------------------
3 main data structure in GO
array

Reference Types:
slice: make or {}
map: make or {}
channel (concurrency): make
slice - dynamic arrar and can be use append method to add more item to this slice. if len < item in slice
  nil slice means declare not initialized 
z:=[]int[]  

map -Key value pair-nil map means declare not initialized 
var m map[int]string

channel

-----------------------------------------------------------
defer- will excute after retrun statement.
Panic- unwanted app crash situation
recover- recover form panic 
--------------------------------------------------------
error handling
error directory
error - built-in type to handle error values
errors - library package
 
err:= errors.New("key doesn't exist")
return err

wrap an error with some context value.
Unwrap
----------------------------------------------------
packages - need to create custom package

--------------------------------------------------
primitive types: string, int, bool, etc
composite types: array, slice and map
 
User-defined types
 
Struct, interface
 
public class Employee : user-defined data structure
{
    state: property or field 
	behavior: method
}
Struct : user-defined data structure
   state: property or field 
   behavior: method
Struct isolates both state and behavior  
Structs created with states
Behaviors can be added later outside the struct the body
no inheritance here in go , have embadeding on struct to another struct 
-- Reciever type
  - Value type(copy new struct every time and same value return)-Pass by value
  - Pointer type - pass by reference 


  concurrency programming 
  - goroutines
  - channels
  ----------------------
M : N Scheduler
M - no. of goroutines
N - no of OS threads
 
1 OS thread can be used for executing several ten thousands of
goroutines
 
goroutines not threads
goroutines are light-weight threads
one goroutine can be started with 4 KB size
func addTable(wg *sync.WaitGroup) {
    // Schedule the call to WaitGroup's Done to tell goroutine is completed.
    defer wg.Done()
    var wgaddTable sync.WaitGroup
    for i := 1; i <= 10; i++ {
       wgaddTable.Add(1)
       go func() {
          defer wgaddTable.Done()
          // do the code
       }()
       sleep := rand.Int63n(1000)
       time.Sleep(time.Duration(sleep) * time.Millisecond)
       fmt.Println("Addition Table for:", i)
       for j := 1; j <= 10; j++ {
          //res = i + j
          fmt.Printf("%d+%d=%d\t", i, j, i+j)
       }
       fmt.Println("\n")
    }
    wgaddTable.Wait()
}
-------------------


"Pipeline" Concurrency Pattern:
 
Problem: Print out fibonacci values of 
10 randomly generated numbers
 
Pipeline with three Goroutines:
 
Goroutine 1: Find out 10 random numbers
Goroutine 2: Find out fibonacci values for the value 
             sent by Goroutine 1
Goroutine 3: Print out fibonacci values for the value 
             sent by Goroutine 2
 
 channel is data structure  and referece type //concurrency folder , matuable for go asyncfronous
channel:make(map({string})string)
buffered and unbuffered

chan<-  Channel direction for send only
<-chan Channel direction for receive only

select

select {
case fib := <-fibs:
    fmt.Printf("Fibonacci value of %d is %d\n", fib.input, fib.value)
case sqr := <-sqrs:
    fmt.Printf("Square value of %d is %d\n", sqr.input, sqr.value)
case <-time.After(time.Millisecond * 10):
    // to do
}
mutex- for stop called by multiple threads


http 

http rest api

"/api/customers", Post => Create new resource on customer (domain entity)
"/api/customers", Get =>  Get all all customers
"/api/customers/{id}", Get =>  Get customer by given id
"/api/customers/{id}", Put =>  Update customer by given id
"/api/customers/{id}", Delete => Delete customer by given id

servemux (net/http)
echo (web framework)
gin (web framework)
 
type Note struct {
	NoteID      string    `json:"noteid,omitempty"` // struct tag
	Title       string    `json:"title"`
	Description string    `json:"description"`
	CreatedOn   time.Time `json:"createdon,omitempty"`
}
 
struct tag is used to provide metadata to be used for encoding packages
when they perform encoding and decoding
 
encoding/json
encoding/xml

reflect/custom DTO . respose retur

Major Go web frameworks:
	gin: https://github.com/gin-gonic/gin (most popular)
	echo: https://github.com/labstack/echo (my recommendation)
gin/echo http wrapper library

GO MODULES
   -> go.mod
         -> go.sum	
 
to install a third-party library:
    go get package_name -> source put into GOMODCACHE
	go get github.com/labstack/echo/v4 	
	go get github.com/gin-gonic/gin
	go get -u github.com/gin-gonic/gin (take latest version)
	go get -u (take latest of all libs)

To update go.mod file:
 
go mod tidy -> synchronise code with go.mod and go.sum
  - Will run:
       -> after referencing/dereferencing a new library
	   -> before taking build
	   -> before committing source code into version control system

UNIT TESTING

https://github.com/stretchr/testify

- red green refactor

standard library packages: testing (foundational library)
						    net/http/httptest (writing unit tests for handlers)	
testing doesn't provide assertion APIs
 
Naming conventions:
 
1: Test suite file, whose name ends with _test.go
    go build or go install, exclude all _test.go files
	"go test -v", command looks into _test.go files and run all unit tests
  "go test -v -cover" to find coverage 
2: Inside _test.go file, write unit test function with:
    func TestXyz(t *testing.T)	{
	}

  Table- testing
  Sub-level test: Within the unit test function, you can have sub tests
for each test case of Table (a collection of test cases) 